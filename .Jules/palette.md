## 2024-05-07 - ARIA labels for icon-only buttons
**Learning:** Icon-only buttons for critical actions (Edit/Delete users, Delete images) often rely on `title` which isn't sufficient for all screen readers.
**Action:** Ensure all icon-only buttons have explicit `aria-label` attributes describing the action.
